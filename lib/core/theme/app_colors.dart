// Fichier centralisé pour toutes les couleurs de l'app
import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales
  static const primary = Color(0xFF4A5FFF); // Bleu principal
  static const primaryDark = Color(0xFF3A4FDF); // Bleu foncé
  static const primaryLight = Color(0xFFE8EBFF); // Bleu clair
  
  // Couleurs neutres
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
  static const grey = Color(0xFF757575);
  static const greyLight = Color(0xFFF5F5F5);
  
  // Couleurs texte
  static const textDark = Color(0xFF1A1A1A);
  static const textGrey = Color(0xFF666666);
  static const textLight = Color(0xFF999999);
  
  // Couleurs accent
  static const success = Color(0xFF4CAF50); // Vert
  static const warning = Color(0xFFFFC107); // Orange
  static const error = Color(0xFFF44336); // Rouge
  
  // Arrière-plans
  static const background = Color(0xFFFAFAFA);
  static const surface = Color(0xFFFFFFFF);
}